#include <bits/stdc++.h>
using namespace std;

string a;
int n;
 
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    cin >> n;
    while (n--)
    {
        cin >> a;
        cout << a.size() << '\n';
    }
    return 0;
}