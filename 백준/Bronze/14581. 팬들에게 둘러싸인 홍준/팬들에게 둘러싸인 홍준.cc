#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main() {
	ios::sync_with_stdio(0);
    cin.tie(0);
    
    string id, fan;
    fan = ":fan:";
    cin >> id;
    
    cout << fan << fan << fan << endl;
    cout << fan << ":" << id << ":" << fan << endl;
    cout << fan << fan << fan;
    
    return 0;
}